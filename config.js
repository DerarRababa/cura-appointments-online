export default {
 //Here we can create any number of environments (and any number of attributes)
  staging: {
    gateway: "https://cura-front-end-test.herokuapp.com",
  },
  production: {
    gateway: "https://cura-front-end-test.herokuapp.com",//this is the same staging gateway. we not have production env yet 
  },
 
};
